export type SaveListEditorProps = {
    id?: string
}

export type EditSaveProps = {
    vsMagic: number
    death: number
    wand: number
    paralysis: number
    breath: number
    spells: number
}